{
  "name": "pokemon-superversion",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:web": "cp -r src/* www/ || true",
    "ci:build": "npm run build:web && npx cap copy android"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  }
}